apiVersion: v1
kind: Service
metadata:
  name: editorial-workflow
  labels:
    app: editorial-workflow
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: editorial-workflow
