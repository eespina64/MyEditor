apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: editorial-workflow

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - ingress.yaml

commonLabels:
  app: editorial-workflow
  version: v1
